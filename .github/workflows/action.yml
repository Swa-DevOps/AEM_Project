on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  build:
    name: Build
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2

      - name: Set up JDK 17
        uses: actions/setup-java@v1
        with:
          java-version: 17
      
      - name: Build and analyze
        env:
          SONAR_TOKEN: squ_ed5dcee470eece11875fa0b612b73b0baaecae37
          SONAR_HOST_URL: http://15.207.87.74:9000/
        run: |
          export PATH=/opt/maven/apache-maven-3.9.6/bin/:$PATH
          mvn clean install sonar:sonar
